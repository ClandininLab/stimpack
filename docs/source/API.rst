API
========

Reference for every public module, generated from the docstrings. ``experiment`` (protocols, data,
the GUI) and ``visual_stim`` (screens, stimuli, rendering) are where most readers want to start;
``locomotion``, ``daq``, ``audio``, ``rpc`` and ``util`` are the layers beneath.

.. toctree::
   :maxdepth: 2

   stimpack.audio
   stimpack.daq
   experiment
   stimpack.locomotion
   stimpack.module
   rpc
   util
   visual_stim
